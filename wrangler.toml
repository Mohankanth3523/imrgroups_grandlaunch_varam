name = "varam-classic-garden"
compatibility_date = "2026-09-23"

# Static-site config for Cloudflare Pages. Run `npm run build` first (or let
# `npm run deploy` do it for you) — this points Wrangler at the build output.
pages_build_output_dir = "dist"
